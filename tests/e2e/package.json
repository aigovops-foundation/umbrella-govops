{
  "name": "umbrella-govops-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright smoke tests for the live Umbrella-GovOps marketing site.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "typescript": "^5.6.0"
  }
}
